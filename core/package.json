{
  "name": "@winter-framework/core",
  "version": "0.0.0",
  "description": "Winter Framework Core",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc --build --force tsconfig.build.json"
  }
}
